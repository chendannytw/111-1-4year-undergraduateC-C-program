#include<stdio.h>
#define SIZE 20

int binarySearch(int b [], int key, int low, int high);
void sort(int a[], int sc[]);
int main(void)
{
	int key;
	
	int a[SIZE] = {2,7,9,10,21,62,63,79,80,92,101,102,123,144,167};
	int s[SIZE] = {};
	int i;
	
	puts("Data items in original order");
	
}

int binarySearch(int b[], int key, int low, int high){
	while( low <= high){
		   int middle = ( low + high) / 2;
		   
		   printfRow(b, low, middle, high);
		   
		   if(searchKey == b[middle]){
		   	  return middle;
		   }
		   
		   else if(searchKey < b[middle]){
		   	       high = middle - 1;
		   }
		   
		   else{
		   	    low = middle + 1;
		   }
	}//end while
	
	return -1; //searchKey not found
}
